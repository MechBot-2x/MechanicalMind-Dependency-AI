name: Runner Maintenance

on:
  schedule:
    - cron: '0 3 * * *'  # Daily at 3 AM

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
    - name: Cleanup old runners
      uses: actions/runner-cleanup@v2
      with:
        token: ${{ secrets.GH_RUNNER_CLEANUP_TOKEN }}
        max-age: 24h  # Remove inactive runners >24h
